# Host folder exposed to DebtRadar as the read-only /workspace scan root.
# Windows example: SCAN_PATH=C:/Users/you/source/my-project
SCAN_PATH=.

# Browser port for the Nginx frontend.
DEBTRADAR_PORT=8080
