const g={A:"badge-a",B:"badge-b",C:"badge-c",D:"badge-d",F:"badge-f"};import { memo } from "react";
export const GradeBadge = memo(function GradeBadge({grade,score,size="sm"}){const c=g[grade]||"bg-surface-3 text-text-muted";const s=size==="lg"?"text-sm px-3 py-1":"text-xs px-2 py-0.5";return<span className={c+" "+s} title={score!=null?"Score: "+score+"/100":undefined}>{grade}{score!=null&&size==="lg"&&<span className="opacity-70 ml-1">({score})</span>}</span>;});const sh={high:"pill-high",medium:"pill-medium",low:"pill-low"};export const SeverityBadge = memo(function SeverityBadge({severity}){return<span className={sh[severity]||"bg-surface-3 text-text-muted"}>{severity}</span>;});export default GradeBadge;